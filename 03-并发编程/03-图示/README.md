# 并发编程图示

本目录暂时只保留目录归属，不提前制作图片。

复杂结构图与流程图将在并发正文完成人工内容审核后补充。后续优先考虑：

- 线程生命周期与中断协作；
- JMM 的可见性、happens-before 与安全发布；
- AQS 同步队列和 Condition 队列；
- ThreadPoolExecutor 的 execute 决策与饱和；
- ThreadLocalMap 的弱引用 key、stale entry 与清理；
- ConcurrentHashMap 的 bin、树化和协作扩容。

图示必须服务于一个明确的 mental model，并从对应文章反向链接；不为了填满目录生成低价值图。
