# Note Style Guide

这是一套面向有经验 Java 后端工程师的复习笔记，不是从零开始的语法教材。

1. **Interview first**：先让读者知道面试如何回答，再进入完整原理。
2. **Conclusion first**：开头先给可复述的结论，并标记 P0/P1/P2 与 Java 版本。
3. **Mental model before detail**：先建立结构、因果和边界，再解释字段或源码。
4. **P0/P1/P2**：导航权重必须显式，不能让低频 API 和核心机制平权。
5. **One source of truth**：定义只维护一份，其他位置解释消费关系并链接回去。
6. **No API dumping**：不把官方 API 枚举当成复习内容；只保留会影响选择和语义的部分。
7. **No giant question lists**：面试题写成因果追问链，不写四十道互不相干的问题。
8. **Mermaid for flow**：流程、决策、状态和简单时序优先 Mermaid。
9. **SVG for structure**：数据结构、引用、桶、指针、队列和对象布局优先自包含 SVG。
10. **Runnable demos inside domains**：完整类和测试放入所属技术域的 `04-示例代码/`；正文只放关键代码。
11. **Version labels**：明确 `JDK 8+`、`Java 17+`、`Java 21+`、`Java 25+`、`Java 26+`，实现细节绑定版本。
12. **README navigation**：根 README 是知识地图；模块 README 是局部路线，不是文件清单堆砌。
13. **Deep dive only when necessary**：长文应该有明确认知主题和源码/规范/工程价值，不按字节数机械拆分。
14. **Avoid duplicate explanations**：重复出现时缩短为上下文说明和链接，不能产生第二套契约。
15. **Generic by default**：正文和默认示例优先使用通用领域模型；WMS、支付、物流等行业案例只能作为可选工程场景，不能成为理解 Java 机制的前置条件。

## 一篇笔记的最低质量线

- 能用一句话说明主题解决的问题；
- 至少有一个容易被追问的“为什么”；
- 区分语言规范、实现细节和工程建议；
- 对可变状态、并发、版本或性能边界不作过度推断；
- 如果声称“可运行”，必须存在对应的 examples 和测试；
- 外部引用、内部链接和图示路径可检查。

## Deep Dive 标准头部

新建或大幅更新深度解析文章时，开头按以下顺序提供信息；已有文章逐步补齐，不要求一次性机械改写：

1. **一句话面试结论**：读者可以在 20～60 秒内复述。
2. **适用版本**：说明 `JDK 8+`、`Java 21+` 或其他版本边界。
3. **Mental model**：用结构、因果或状态变化解释为什么。
4. **本章边界**：明确本章不负责的契约，链接到唯一权威文章。
5. **关键源码路径**：只列会改变理解的字段、方法和调用链。
6. **验证入口**：链接到图示、示例代码和测试；没有可运行验证时要明确说明。

## 新知识点新增步骤

新增一个主题时，先确定它属于哪个技术域，再判断需要哪些表达层：

1. 在该模块的 `01-面试速记/` 建立可复述的面试入口；
2. 在 `02-深度解析/` 放规范边界、源码路径或工程取舍；
3. 只有确实能降低理解成本时，才在 `03-图示/主题/` 增加图；
4. 需要可重复验证时，在 `04-示例代码/` 增加完整主类和测试；
5. 更新模块 README 的主题资源地图，并从文章链接到图、示例和测试；
6. 检查相对链接、图像、代码围栏、测试和根目录 `mvnw test`；Windows 使用 `mvnw.cmd test`。

不要求每个主题机械地创建四种文件，但每个已经创建的资源都必须有明确主题归属；没有归属的共享目录不作为默认落点。

## 图示规则

- 图必须同时具备技术域和知识主题归属，路径优先为 `<模块>/03-图示/<主题>/`。
- Mermaid 用于流程、决策、生命周期、状态机和简单时序；它应靠近解释文字。
- SVG 用于 bucket、链表、引用、指针、队列和对象布局等空间结构；SVG 必须自包含、可缩放、无需外部字体、图片或 base64 资源。
- PNG/JPG 只在已有位图确实提供不可替代的信息时保留；新建技术结构图默认优先 SVG。
- 每张图只解决一个明确问题，必须有清晰标题和可读文字，不逐字重复正文。
- 提交前检查 XML 合法、相对链接正确、GitHub 可直接渲染，并确认主题文章确实链接到它。

## Demo 规则

- 完整 Demo 必须属于具体知识模块和主题，不能按“所有示例”长期堆在根目录。
- Demo 必须有明确的 `main` 入口或可调用主类、对应 Test 和稳定的验证断言。
- Deep Dive 或 Interview Review 应链接到 Demo；Demo 的 README 或模块 README 应反向定位文章。
- Demo 应能在 Java 21 + Maven + JUnit 5 环境中独立运行，不依赖未声明的外部服务。
- 代码复用不能破坏学习路径；宁可保留小而清晰的主题示例，也不要抽象出读者无法定位的共享示例层。

## 写作禁区

- 不把 `int`、`for`、`if` 等常识写成主线，除非涉及语义陷阱或底层机制；
- 不为了凑章节强行拆成几十个小文件；
- 不把仓库中的工程案例改写成未经证实的个人生产事故或个人项目经历；
- 不复制其他公开仓库的大段文字、图片或代码。
- 不在公开正文中保留公司、系统、接口、租户或业务协议等无法由通用读者理解的内部标识；确有教学价值时，改写为脱敏的抽象案例。
- 行业案例必须先给出通用结论，再标记为“可选工程案例”，并说明它不代表任何特定公司的真实系统。

## 文件与目录命名

1. 面向用户阅读的目录使用简体中文。
2. Markdown 文件使用简体中文语义名称。
3. Java/JVM/API/类名等正式技术标识保留英文。
4. 不使用全英文 slug。
5. 不使用拼音。
6. 不为了中文化翻译 Java API 名称。
7. 文件名应直接表达技术主题。
8. Java 源文件继续遵循 Java 英文命名规范。
9. Maven/GitHub 等工具约定路径保持标准名称。
